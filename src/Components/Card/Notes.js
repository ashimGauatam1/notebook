import React from 'react'


// import Noteitem from '../Noteitem';
const Notes = () => {
  return (
   <div>
      {/* { 
      notes.map((note)=>{
        return <Noteitem note={note}/>
       })} */}
    </div>
  )
}

export default Notes
